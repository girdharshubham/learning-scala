package edu.self.leetcode

import org.scalatest.flatspec.AnyFlatSpec
import org.scalatest.matchers.should.Matchers

class StringsTest extends AnyFlatSpec with Matchers {
  val str1 = "ab"
  val str2 = "ba"

//  val permutation: Permutation.type = Permutation

  it should "be able to check if a permutation exists or not" in {
//    permutation.checkInclusion(str1, str2) shouldBe true
  }

}
